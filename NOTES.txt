when the user selects apple calendar login, the form needs to come up just as it does with caldav, instead of using the sign in wth apple button. however the caldav url needs to be prefilled with the url 'https://caldav.icloud.com/', so that the user can't get rid of it, but they can add more characters to the input.. this obviously will login the normal 'caldav' way, but using apple.. for example using a text widget with the 'https://caldav.icloud.com/', then using a textfield, but the text widget does not have any border on the left hand side, the textfield does not have any border or margin on the right side, and they can be placed together, so they look like they are not separate?
